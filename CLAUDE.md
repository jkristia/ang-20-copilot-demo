# Claude Instructions

## After any change to client code

Run the Angular build to verify compilation:

```sh
cd client && npx ng build --configuration development
```

The build must succeed (no errors) before considering a task done.
